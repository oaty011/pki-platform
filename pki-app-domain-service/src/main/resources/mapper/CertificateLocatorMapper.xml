<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pki.platform.appdomain.mapper.CertificateLocatorMapper">

    <resultMap id="certificateLocatorResultMap" type="com.pki.platform.appdomain.model.CertificateLocator">
        <id column="cert_serial" property="certSerial" />
        <id column="issuer_id" property="issuerId" />
        <result column="subject_id" property="subjectId" />
        <result column="shard_id" property="shardId" />
        <result column="storage_type" property="storageType" />
        <result column="is_revoked" property="revoked" />
        <result column="routing_version" property="routingVersion" />
        <result column="revoked_at" property="revokedAt" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <insert id="insert" parameterType="com.pki.platform.appdomain.model.CertificateLocator">
        INSERT INTO pki_app.certificate_locator (
            cert_serial, issuer_id, subject_id, shard_id, storage_type, is_revoked, routing_version, revoked_at, created_at, updated_at
        ) VALUES (
            #{certSerial}, #{issuerId}, #{subjectId}, #{shardId}, #{storageType}, #{revoked}, #{routingVersion}, #{revokedAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="selectByCertSerialAndIssuerId" resultMap="certificateLocatorResultMap">
        SELECT cert_serial, issuer_id, subject_id, shard_id, storage_type, is_revoked, routing_version, revoked_at, created_at, updated_at
        FROM pki_app.certificate_locator
        WHERE cert_serial = #{certSerial}
          AND issuer_id = #{issuerId}
    </select>
</mapper>
