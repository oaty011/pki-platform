<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pki.platform.ecudomain.mapper.SystemBootstrapInfoMapper">

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM pki_ecu.system_bootstrap_info
    </select>
</mapper>
